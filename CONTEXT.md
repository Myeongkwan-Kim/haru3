---
documentLanguage: ko
---

# 하루 세 아이 — 프로젝트 맥락

유치원 교사가 아이를 관찰한 내용을 짧게 남기고, 그 기록을 나중에 대시보드로
보기 위한 실험용 도구입니다. 2026년 9월 현재 **2주 파일럿을 앞둔 1차 프로토타입**
단계이며, 실사용자는 유치원 교사 1명입니다.

## 이 도구가 풀려는 문제

한 반에 아이가 10명대만 되어도 교사가 아이별로 기록을 남기는 일은 실패합니다.
몸을 많이 쓰는 직업이라 손이 자유롭지 않고, 기록해야 한다는 사실 자체를 잊습니다.
그래서 이 도구는 **기록의 양을 줄이는 방향**으로 설계되었습니다. 자세한 근거는
`docs/adr/`에 있습니다.

## 도메인 용어

| 용어 | 뜻 |
|---|---|
| **슬롯(slot)** | 기록을 남기는 시간대. 기본값은 오전 자유놀이 / 오전 활동 / 점심·낮잠 / 오후 활동. 관찰이 일어나는 시간(쉬는 시간)과 기록하는 시간(수업 시간)이 다르기 때문에, 기록은 항상 30분~1시간 뒤의 회상이다. 슬롯은 그 회상의 단위다. |
| **관찰 태그** | "무슨 일이 있었나"에 해당하는 **사실** 필드. 여러 개 선택 가능. |
| **상태 축(mood)** | "선생님이 본 아이 상태"에 해당하는 **해석** 필드. -2(위축) ~ +2(활발)의 5단계. |
| **공백** | 어떤 아이에 대해 일정 기간 기록이 없는 상태. 결측이 아니라 그 자체가 신호로 취급되며, 첫 화면 상단의 "놓치고 있는 아이"가 이것을 표시한다. |
| **기록(entry)** | `날짜 + 슬롯 + 아이` 조합 하나당 최대 한 건. 같은 조합을 다시 열면 수정이 된다. |

## 기술 규약

- **단일 정적 HTML + `localStorage`.** 서버가 없고 앞으로도 붙이지 않습니다.
  저장 키는 `haru3.v1`입니다.
- **외부 라이브러리를 추가하지 않습니다.** 외부 요청은 Google Fonts 뿐이며,
  기록 데이터는 어떤 경로로도 외부에 나가지 않습니다.
- **`index.html`이 원본입니다.** Claude Artifact 본문 규격이라 `<!doctype>`,
  `<head>`, `<body>` 태그가 없습니다. 이것은 결함이 아닙니다.
- **`dist/`는 산출물입니다.** `./build.sh`가 원본을 완전한 HTML 문서로 감싸
  생성합니다. `dist/`를 직접 고치지 마세요.
- ES5 문법으로 작성되어 있습니다. 빌드 도구가 없으므로 트랜스파일 단계가 없습니다.

## 이 단계에서 하지 않는 것

- 서버 저장, 계정, 여러 교사의 공동 작업
- 학부모에게 보이는 화면 — 초기 기록은 교사 개인의 것이어야 솔직하게 남습니다
- 추천 시스템, 생성형 요약 — 데이터가 쌓인 뒤의 다음 단계입니다

## 파일럿에서 확인할 것

1. 하루에 실제로 몇 명을 기록하는가 (0명인 날이 며칠인가)
2. 어느 시간대에 기록하는가
3. 이모지·태그만으로 충분한가, 결국 한 줄 메모를 쓰게 되는가
4. "놓치고 있는 아이"를 보고 실제로 그 아이를 기록하러 가는가
